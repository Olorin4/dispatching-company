import "./normalize.css";
import "./styles.css";
import Carousel from "component-image-carousel/src/img-carousel.js";

// Initialize the image carousel when the DOM is fully loaded
document.addEventListener("DOMContentLoaded", () => {
    initImageCarousel();
});
