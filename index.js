import { initImageCarousel } from 'component-image-carousel';

// Initialize the image carousel when the DOM is fully loaded
document.addEventListener('DOMContentLoaded', () => {
    initImageCarousel();
});